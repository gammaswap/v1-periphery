name: Node.js Package

on:
  push:
    branches:
      - main

jobs:
  publish-gpr:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{secrets.MY_TOKEN}}
      - name: Step 2 - GitHub Action if statement (true)
        env: 
          WHO_TO_TRUST: ${{ secrets.SECRET_TOKEN }}
          WHO_TO_TRUST1: ${{ secrets.MY_TOKEN }}
        if:  env.WHO_TO_TRUST == env.WHO_TO_TRUST1
        run: echo "I know what the secret token is!"
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 16
          registry-url: https://npm.pkg.github.com/
          scope: '@gammaswap'
      - run: npm install
      - run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{secrets.GS_SECRET}}
